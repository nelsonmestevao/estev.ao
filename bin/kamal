#!/usr/bin/env bash

set -euo pipefail

KAMAL_BIN="$(ruby -e 'puts Gem.user_dir + "/bin/kamal"')"

if [[ ! -x "$KAMAL_BIN" ]]; then
  echo "executable not found at $KAMAL_BIN" >&2
  exit 1
fi

exec "$KAMAL_BIN" "$@"
